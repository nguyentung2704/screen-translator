<Window x:Class="ScreenTranslator.UI.Views.DebugLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:ScreenTranslator.UI.ViewModels"
        mc:Ignorable="d"
        Title="Debug Log" Height="400" Width="600"
        Background="#1E1E1E" Foreground="White">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding LogCount, StringFormat='{}{0} lines'}" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <CheckBox Content="Auto-scroll" IsChecked="{Binding AutoScroll}" Foreground="#AAAAAA" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="{Binding IsPaused, Converter={StaticResource BooleanToVisibilityConverter}}" 
                        Command="{Binding TogglePauseCommand}" Margin="0,0,5,0" Padding="10,5">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Content" Value="Pause"/>
                            <Setter Property="Background" Value="#333"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPaused}" Value="True">
                                    <Setter Property="Content" Value="Resume"/>
                                    <Setter Property="Background" Value="#3498DB"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <Button Content="Save" Command="{Binding SaveCommand}" Margin="0,0,5,0" Padding="10,5" Background="#333" Foreground="White"/>
                <Button Content="Clear" Command="{Binding ClearCommand}" Padding="10,5" Background="#333" Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Logs -->
        <Border Grid.Row="1" BorderBrush="#333" BorderThickness="1" Background="#000000">
            <ListBox ItemsSource="{Binding Logs}" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingStackPanel.IsVirtualizing="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MinWidth="80"/>
                                <ColumnDefinition Width="Auto" MinWidth="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}" Foreground="#666" Margin="0,0,10,0" FontFamily="Consolas"/>
                            
                            <TextBlock Grid.Column="1" x:Name="TypeBlock" Text="{Binding Type, StringFormat='{}:'}" Margin="0,0,10,0" FontWeight="Bold" FontFamily="Consolas"/>
                            
                            <TextBlock Grid.Column="2" Text="{Binding Content}" Foreground="#DDD" TextWrapping="Wrap" FontFamily="Consolas"/>
                        </Grid>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Type}" Value="OCR Text">
                                <Setter TargetName="TypeBlock" Property="Foreground" Value="#F1C40F"/> <!-- Yellow -->
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="Translate">
                                <Setter TargetName="TypeBlock" Property="Foreground" Value="#2ECC71"/> <!-- Green -->
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,5,0,0">
            <TextBlock Text="{Binding StatusText}" Foreground="#555" FontSize="10"/>
        </Grid>
    </Grid>
</Window>
